import XCTest
import _Differentiation

@testable import DifferentiableSwiftExample

final class DifferentiableSwiftExampleTests: XCTestCase {
  func testExample() throws {
    let (value, pullback) = valueWithPullback(at: [0, 1, 2], of: foo)
    array

  }
}
